# These sources depend on targets from "//content/browser", which means they can
# only be loaded on Blink builds.
# NOTE: If/when targets used on iOS are refactored into a separate BUILD file,
# this check can be removed.
